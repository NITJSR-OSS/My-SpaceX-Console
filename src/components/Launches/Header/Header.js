import React from "react";
import styles from "./Header.css";

const Header = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.heading}>LAUNCHES</h1>
    </div>
  );
};

export default Header;
